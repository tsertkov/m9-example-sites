# Example site with LambCI based CD pipeline

1. Read setup instructions in `cf-ci/README.md` to create static site infrastructure.

2. Update configuration values in `config.json`.

3. Deploy serverless pipeline with `sls deploy`.
