# m9-generator-serverless-deploy

Serverless function executing m9-generator deploy task in AWS lambda.

```sh
$ yarn
$ yarn run invoke-local-docker
$ yarn run invoke-local

$ sls deploy
$ yarn run invoke
```
